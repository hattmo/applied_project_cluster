# Image versions and namespace
# CI reads image tags, ArgoCD deploys with these values

namespace: npc

images:
  controller:
    tag: "0.1.0"
  openclaw:
    tag: "0.1.0"
  vmwareGateway:
    tag: "0.1.0"
