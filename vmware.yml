apiVersion: v1
kind: Pod
metadata:
  name: vmware-gateway
  namespace: npc
  labels:
    app: vmware
spec:
  containers:
  - name: app
    image: hattmo/vmware_gateway
    envFrom:
    - secretRef:
        name: vmware-creds
---
apiVersion: v1
kind: Service
metadata:
  name: vmware-gateway-service
  namespace: npc
spec:
  type: ClusterIP
  selector:
    app: vmware
  ports:
  - port: 80
    targetPort: 8888

